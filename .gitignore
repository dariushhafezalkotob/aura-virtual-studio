node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
.npm-cache
.DS_Store
dist-server

# Working data: projects, generated assets, takes and motion.
# Not source - it is 300 MB+ and changes on every generation. Back it up with
# `npm run backup:data`, not with git. Older versions are still in git history.
data/
backups/

# Local development database (binaries + data, ~400 MB)
.mongo/
